# 部署到阿里云服务器

面向「你和几个朋友一起用」的部署方式：Docker + Caddy 自动 HTTPS。

> **先读这一段。** 这个应用里全是你的私人信息（手机号、邮箱、完整简历、面试录音转写）。
> 代码里已经加了登录门禁，**但请务必按本文档配好 `.env` 里的 `SECRET_KEY`**，
> 否则容器重建后所有人的登录态会失效，而且签名密钥是临时生成的。

---

## 一、买服务器

| 项 | 建议 | 说明 |
|---|---|---|
| 地域 | 国内（北京/杭州等）或 香港 | 国内延迟低但要备案；香港免备案、可立即绑域名 |
| 规格 | **2 核 2G** 起 | 应用本身很轻（数据 6 MB、单进程）；内存主要留给 PDF 渲染 |
| 系统 | Ubuntu 22.04 / 24.04 64 位 | 或 Alibaba Cloud Linux 3 |
| 带宽 | 按量 3~5 Mbps 起 | 语音是流式的，带宽太小会卡顿 |
| 系统盘 | 40G 云盘 | 够用很久 |

**安全组要放行的端口**：

| 端口 | 用途 | 谁能访问 |
|---|---|---|
| 22 | SSH | 建议只放行你自己的 IP |
| 80 | HTTP，仅用于申请证书和跳转 | 所有人 |
| 443 | HTTPS，真正的入口 | 所有人 |

⚠️ **不要放行 7870**。应用只在 Docker 内网监听，对外只有 Caddy 的 443。
放行了就等于绕过 HTTPS，手机麦克风也用不了。

---

## 二、服务器上装 Docker

```bash
# 阿里云 Ubuntu，一条命令
curl -fsSL https://get.docker.com | sh

# 让当前用户不用 sudo 也能用 docker（重新登录后生效）
sudo usermod -aG docker $USER
newgrp docker

# 验证
docker --version && docker compose version
```

---

## 三、上传代码

方式任选：

```bash
# A. 用 git（推荐，更新方便）
sudo apt install -y git
git clone https://github.com/flingjie/Agent-100-Days.git
cd Agent-100-Days/product/job_assistant

# B. 从本地传（在你自己电脑上执行）
rsync -av --exclude '.venv' --exclude 'data' --exclude '__pycache__' \
  product/job_assistant/ root@你的公网IP:/opt/job-assistant/
```

---

## 四、写配置文件

```bash
cd /opt/job-assistant        # 或你实际的目录
cp .env.example .env
```

生成一个会话密钥，填进 `.env`：

```bash
python3 -c "import secrets; print(secrets.token_urlsafe(48))"
```

然后编辑 `.env`，**这几项必填**：

```ini
DEEPSEEK_API_KEY=sk-你的key
DASHSCOPE_API_KEY=sk-你的key

# 上面生成的那串
SECRET_KEY=xxxxxxxxxxxxxxxxxxxxxxxx

# 管理员手机号——登录靠它，不填就没人能进
ADMIN_PHONE=13800000000

# 阿里云短信（下面「手机号登录」一节详述）
ALIYUN_ACCESS_KEY_ID=LTAI...
ALIYUN_ACCESS_KEY_SECRET=...
SMS_SIGN_NAME=你的短信签名
SMS_TEMPLATE_CODE=SMS_xxxxxxx
```

其余的保持默认即可。**备案还没下来时**，`SITE_ADDRESS=:443`、`CADDYFILE=Caddyfile.ip` 不用改。

---

## 五、启动

```bash
docker compose up -d --build
```

第一次会构建镜像（3~5 分钟，主要在装依赖）。看状态和日志：

```bash
docker compose ps
docker compose logs -f app       # Ctrl+C 退出查看，不影响服务
```

看到 `已创建初始管理员账号 admin` 就说明起来了。

访问 **`https://你的公网IP`**：

- 浏览器会警告「证书不受信任」→ 点「高级」→ 「继续前往」
- 这是自签证书的正常现象，**不影响任何功能**（手机麦克风也照常能用）
- 用你在 `.env` 里设的 `ADMIN_PHONE` 收验证码登录

**没配短信也能用**：验证码会打印到服务端日志，从日志里抄一次即可：

```bash
docker compose -f docker-compose.behind-nginx.yml logs app | grep 验证码 | tail -1
```

---

## 五点五、手机号登录与阿里云短信

登录方式是**手机号 + 验证码**，没注册过的号首次登录自动建账号。

### 需要 4 个配置

| 环境变量 | 阿里云控制台对应物 |
|---|---|
| `ALIYUN_ACCESS_KEY_ID` | RAM 子账号 AccessKey（只授 `dysmsapi:SendSms` 最小权限） |
| `ALIYUN_ACCESS_KEY_SECRET` | 同上（Secret 只显示一次） |
| `SMS_SIGN_NAME` | 短信签名（2~8 字） |
| `SMS_TEMPLATE_CODE` | 验证码模板 Code，模板里必须有 `${code}` 变量 |

代码传的模板参数是 `{"code":"123456"}`，所以**模板变量名必须叫 `code`**。

> 这套变量名和 `video_note` 项目完全一致，那边的配置可以直接搬过来。

### 资质流程（这是最长的一环）

1. **企业实名认证**（个体户也算）——个人认证账号申请不了正式签名
2. 开通短信服务（按量付费，国内验证码约 0.045 元/条）
3. 申请**资质**（约 2 工作日）
4. 申请**签名**（审核约 2 小时 + **运营商报备 5~7 工作日**）← 最慢，尽早启动
5. 申请**模板**（约 2 工作日）
6. 建 RAM 子账号拿 AccessKey

### 没配短信也能用

四个变量缺任意一个，系统就退回「把验证码打进日志」模式：

```bash
docker compose -f docker-compose.behind-nginx.yml logs app | grep 验证码
# [验证码] 13800000000 → 123456（5 分钟内有效）
```

配齐之后**自动切换**到真实短信，不用改代码、不用改数据库。

### 防刷

| 限制 | 值 | 为什么 |
|---|---|---|
| 同号重发间隔 | 60 秒 | 界面上的倒计时和它对齐 |
| 同号每小时上限 | 8 条 | 60 秒间隔挡不住"每分钟一条刷一小时" |
| 有效期 | 5 分钟 | |
| 一次性 | 用过即废 | 防重放 |
| 重新发送 | 作废上一条 | 否则用户输旧的那条会莫名失败 |

### ⚠️ 被锁在外面怎么办

手机号登录没有密码，**短信挂了就进不去**。所以留了两条后路：

**后路 1：看日志里的验证码**（短信服务故障，但服务器还活着）

```bash
docker compose -f docker-compose.behind-nginx.yml logs app | grep 验证码 | tail -1
```

**后路 2：直接从库里登进去**（连日志都进不去时）

```bash
cd /opt/resume_assistant
# 临时把某个号加成管理员
docker compose -f docker-compose.behind-nginx.yml exec app python -c "
import sys; sys.path.insert(0,'/app')
from app import auth
with auth.center() as c:
    c.execute('UPDATE users SET phone=?, is_admin=1 WHERE id=1', ('13800000000',))
print('已改绑')
"
```

**后路 3：从数据库里读验证码**（验证码发不出去，但你想立刻登进去）

```bash
docker compose -f docker-compose.behind-nginx.yml exec app python -c "
import sys; sys.path.insert(0,'/app')
from app import auth
with auth.center() as c:
    for r in c.execute('SELECT phone, code, expires_at FROM sms_code ORDER BY id DESC LIMIT 5'):
        import time
        print(f'{r[\"phone\"]}  {r[\"code\"]}  {\"有效\" if r[\"expires_at\"]>time.time() else \"过期\"}')
"
```

## 六、让朋友用

### 手机号登录

**登录就是注册**：朋友打开网址 → 填手机号 → 收验证码 → 登录。没注册过的号会自动建账号。

前提是短信服务配好（下一节）。没配的话验证码只打在服务器日志里，朋友自己拿不到。

**每个人的数据是物理隔离的**：一人一个 `.db` 文件，谁也看不到谁的简历。

### 关掉注册（推荐用完就关）

国内公网 IP 会被扫描器扫到。让朋友都注册完之后，建议关掉注册：

```bash
docker compose exec app python -c "
import sys; sys.path.insert(0,'/app')
from app import auth
auth.set_setting('registration_open', '0')
print('已关闭注册')
"
```

关掉之后，新用户注册会返回「管理员已关闭注册」，已有账号不受影响。
想再开就把 `'0'` 改成 `'1'`。

### 控制 AI 额度

你选的是**朋友们共用你的 key**，所以每人每天有调用上限（默认 300 次）：

```bash
# 看所有人今天用了多少
docker compose exec app python -c "
import sys; sys.path.insert(0,'/app')
from app import auth
for u in auth.usage_by_user():
    print(f\"  {u['display_name']:10} 今日 {u['today_calls']:>4} 次 / 上限 {u['daily_quota']}  累计 {u['total_calls']}\")
"
```

调某个人的额度（比如给他提到 1000）：

```bash
docker compose exec app python -c "
import sys; sys.path.insert(0,'/app')
from app import auth
with auth.center() as c:
    c.execute('UPDATE users SET daily_quota = 1000 WHERE username = ?', ('xiaoming',))
print('已调整')
"
```

额度按**自然日**算，每天 0 点自动重置（时区在 compose 里设成了 Asia/Shanghai）。

---

## 七、备案下来之后：换成域名 + 真证书

前提：**ICP 备案已通过**，且域名的 A 记录已解析到这台服务器。

1. 改 `.env`：

```ini
SITE_ADDRESS=job.你的域名.com
ACME_EMAIL=你的邮箱
CADDYFILE=Caddyfile.domain
```

2. 重启代理：

```bash
docker compose up -d caddy
docker compose logs -f caddy     # 看证书申请过程
```

几十秒后 `https://job.你的域名.com` 就是**真证书**了，浏览器不再有任何警告。
Let's Encrypt 证书 90 天到期，Caddy 会自动续，不用管。

3. 确保 `COOKIE_SECURE=1`（`.env.example` 里默认就是），这样登录 cookie 只在加密连接里发送。

---

## 八、日常运维

```bash
# 更新到最新代码（改了代码要 --build，见下面说明）
git pull
docker compose up -d --build

# 重启
docker compose restart app

# 看实时日志
docker compose logs -f app

# 备份（数据全在 ./data 里，拷走就行）
tar czf backup-$(date +%F).tar.gz data/

# 恢复
tar xzf backup-2026-09-13.tar.gz
docker compose restart app
```

> ⚠️ **改了代码必须 `--build`，不能只 `restart`。**
> `Dockerfile` 里是 `COPY app/ ./app/`，代码被**打进镜像**了。
> `docker compose restart app` 只是重启容器，跑的还是旧镜像里的旧代码——
> 我在部署登录页时就踩了这个坑：文件传上去了、服务也重启了，
> 但线上还是旧页面 `login.css` 直接 404。

### 建议加个定时备份

```bash
crontab -e
# 每天凌晨 3 点备份，保留最近 14 天
0 3 * * * cd /opt/job-assistant && tar czf /root/backups/ja-$(date +\%F).tar.gz data/ && find /root/backups -name 'ja-*.tar.gz' -mtime +14 -delete
```

（记得先 `mkdir -p /root/backups`）

---

## 九、出问题怎么查

| 现象 | 原因 / 处理 |
|---|---|
| 打不开，`docker compose ps` 显示 app 一直 restarting | `docker compose logs app` 看报错。多半是 `.env` 里的 key 填错格式 |
| 收不到验证码 | 先看 `logs app \| grep 验证码`——如果日志里有，说明短信没配好（走的兜底）；如果日志里没有，看是不是被 60 秒间隔或每小时 8 条的上限挡住了 |
| 短信发送失败 | 检查签名/模板是否审核通过、是否还在运营商报备期（报备要 5~7 工作日） |
| 改完 ADMIN_PHONE 重启后登不进去 | ADMIN_PHONE 只在**首次建号**和**从旧版迁移**时用。改绑要手动：见上一节「后路 2」 |
| 手机按麦克风没反应 | 必须走 HTTPS。检查地址栏是不是 `https://`（自签证书也算） |
| 语音很卡、断断续续 | 带宽不够，或者 Caddy 在缓冲。确认 `Caddyfile` 里有 `flush_interval -1` |
| 提示「今天的 AI 用量已达上限」 | 正常，说明配额在起作用。按第六节调高 |
| 传截图后一直「识别中」 | 视觉模型慢（一张图约 13 秒），等就行。超过 2 分钟看 `logs` 里有没有 API 报错 |
| 忘记/换手机号了 | 见上一节「后路 2」，直接把 `users` 表里的 phone 改掉 |

---

## 十、这套配置的安全边界

已经做了：

- ✅ 登录门禁（签名 cookie，30 天有效）
- ✅ 手机验证码登录（一次性、5 分钟过期、60 秒间隔、每小时 8 条上限）
- ✅ **没有密码**——不存在密码泄露、撞库、弱口令这些问题
- ✅ **数据物理隔离**：一人一个数据库文件
- ✅ API key 只存在服务端，前端拿不到
- ✅ 每人每日调用配额，防止共用 key 被烧穿
- ✅ HTTPS（备案前自签、备案后真证书）
- ✅ 安全响应头、关闭搜索引擎收录
- ✅ 容器日志限 10MB × 3，不会写满磁盘

**还没做的**（如果你要给别人长期用，建议补上）：

- ⚠️ **验证码校验没有失败次数限制**——6 位数字穷举需要 100 万次，配合 5 分钟过期实际不可行，但严格说可以再加一层
- ⚠️ **手机号即身份**：换号要走「后路 2」手动改库
- ⚠️ **开放注册期间**，任何知道网址的人都能注册（第六节有说明怎么关）
- ⚠️ 上传文件没有做病毒扫描（只校验了扩展名和大小）

对「自己和几个朋友用」这个场景，现在的强度是够的。
