# 部署到阿里云服务器

面向「你和几个朋友一起用」的部署方式：Docker + Caddy 自动 HTTPS。

> **先读这一段。** 这个应用里全是你的私人信息（手机号、邮箱、完整简历、面试录音转写）。
> 代码里已经加了登录门禁，**但请务必按本文档配好 `.env` 里的 `SECRET_KEY`**，
> 否则容器重建后所有人的登录态会失效，而且签名密钥是临时生成的。

---

## 一、买服务器

| 项 | 建议 | 说明 |
|---|---|---|
| 地域 | 国内（北京/杭州等）或 香港 | 国内延迟低但要备案；香港免备案、可立即绑域名 |
| 规格 | **2 核 2G** 起 | 应用本身很轻（数据 6 MB、单进程）；内存主要留给 PDF 渲染 |
| 系统 | Ubuntu 22.04 / 24.04 64 位 | 或 Alibaba Cloud Linux 3 |
| 带宽 | 按量 3~5 Mbps 起 | 语音是流式的，带宽太小会卡顿 |
| 系统盘 | 40G 云盘 | 够用很久 |

**安全组要放行的端口**：

| 端口 | 用途 | 谁能访问 |
|---|---|---|
| 22 | SSH | 建议只放行你自己的 IP |
| 80 | HTTP，仅用于申请证书和跳转 | 所有人 |
| 443 | HTTPS，真正的入口 | 所有人 |

⚠️ **不要放行 7870**。应用只在 Docker 内网监听，对外只有 Caddy 的 443。
放行了就等于绕过 HTTPS，手机麦克风也用不了。

---

## 二、服务器上装 Docker

```bash
# 阿里云 Ubuntu，一条命令
curl -fsSL https://get.docker.com | sh

# 让当前用户不用 sudo 也能用 docker（重新登录后生效）
sudo usermod -aG docker $USER
newgrp docker

# 验证
docker --version && docker compose version
```

---

## 三、上传代码

方式任选：

```bash
# A. 用 git（推荐，更新方便）
sudo apt install -y git
git clone https://github.com/flingjie/Agent-100-Days.git
cd Agent-100-Days/product/job_assistant

# B. 从本地传（在你自己电脑上执行）
rsync -av --exclude '.venv' --exclude 'data' --exclude '__pycache__' \
  product/job_assistant/ root@你的公网IP:/opt/job-assistant/
```

---

## 四、写配置文件

```bash
cd /opt/job-assistant        # 或你实际的目录
cp .env.example .env
```

生成一个会话密钥，填进 `.env`：

```bash
python3 -c "import secrets; print(secrets.token_urlsafe(48))"
```

然后编辑 `.env`，**这四项必填**：

```ini
DEEPSEEK_API_KEY=sk-你的key
DASHSCOPE_API_KEY=sk-你的key

# 上面生成的那串
SECRET_KEY=xxxxxxxxxxxxxxxxxxxxxxxx

# 初始管理员密码，登录后请立刻改掉
ADMIN_PASSWORD=你自己定一个强密码
```

其余的保持默认即可。**备案还没下来时**，`SITE_ADDRESS=:443`、`CADDYFILE=Caddyfile.ip` 不用改。

---

## 五、启动

```bash
docker compose up -d --build
```

第一次会构建镜像（3~5 分钟，主要在装依赖）。看状态和日志：

```bash
docker compose ps
docker compose logs -f app       # Ctrl+C 退出查看，不影响服务
```

看到 `已创建初始管理员账号 admin` 就说明起来了。

访问 **`https://你的公网IP`**：

- 浏览器会警告「证书不受信任」→ 点「高级」→「继续前往」
- 这是自签证书的正常现象，**不影响任何功能**（手机麦克风也照常能用）
- 用 `admin` + 你在 `.env` 里设的 `ADMIN_PASSWORD` 登录

登录后第一件事：**「我的 → 设置 → 改密码」**，然后把 `.env` 里的 `ADMIN_PASSWORD` 删掉。

---

## 六、让朋友用

### 开放注册

默认就是开放的。朋友打开 `https://你的IP` → 点「注册」→ 填用户名密码即可。

**每个人的数据是物理隔离的**：一人一个 `.db` 文件，谁也看不到谁的简历。

### 关掉注册（推荐用完就关）

国内公网 IP 会被扫描器扫到。让朋友都注册完之后，建议关掉注册：

```bash
docker compose exec app python -c "
import sys; sys.path.insert(0,'/app')
from app import auth
auth.set_setting('registration_open', '0')
print('已关闭注册')
"
```

关掉之后，新用户注册会返回「管理员已关闭注册」，已有账号不受影响。
想再开就把 `'0'` 改成 `'1'`。

### 控制 AI 额度

你选的是**朋友们共用你的 key**，所以每人每天有调用上限（默认 300 次）：

```bash
# 看所有人今天用了多少
docker compose exec app python -c "
import sys; sys.path.insert(0,'/app')
from app import auth
for u in auth.usage_by_user():
    print(f\"  {u['display_name']:10} 今日 {u['today_calls']:>4} 次 / 上限 {u['daily_quota']}  累计 {u['total_calls']}\")
"
```

调某个人的额度（比如给他提到 1000）：

```bash
docker compose exec app python -c "
import sys; sys.path.insert(0,'/app')
from app import auth
with auth.center() as c:
    c.execute('UPDATE users SET daily_quota = 1000 WHERE username = ?', ('xiaoming',))
print('已调整')
"
```

额度按**自然日**算，每天 0 点自动重置（时区在 compose 里设成了 Asia/Shanghai）。

---

## 七、备案下来之后：换成域名 + 真证书

前提：**ICP 备案已通过**，且域名的 A 记录已解析到这台服务器。

1. 改 `.env`：

```ini
SITE_ADDRESS=job.你的域名.com
ACME_EMAIL=你的邮箱
CADDYFILE=Caddyfile.domain
```

2. 重启代理：

```bash
docker compose up -d caddy
docker compose logs -f caddy     # 看证书申请过程
```

几十秒后 `https://job.你的域名.com` 就是**真证书**了，浏览器不再有任何警告。
Let's Encrypt 证书 90 天到期，Caddy 会自动续，不用管。

3. 确保 `COOKIE_SECURE=1`（`.env.example` 里默认就是），这样登录 cookie 只在加密连接里发送。

---

## 八、日常运维

```bash
# 更新到最新代码
git pull
docker compose up -d --build

# 重启
docker compose restart app

# 看实时日志
docker compose logs -f app

# 备份（数据全在 ./data 里，拷走就行）
tar czf backup-$(date +%F).tar.gz data/

# 恢复
tar xzf backup-2026-09-13.tar.gz
docker compose restart app
```

### 建议加个定时备份

```bash
crontab -e
# 每天凌晨 3 点备份，保留最近 14 天
0 3 * * * cd /opt/job-assistant && tar czf /root/backups/ja-$(date +\%F).tar.gz data/ && find /root/backups -name 'ja-*.tar.gz' -mtime +14 -delete
```

（记得先 `mkdir -p /root/backups`）

---

## 九、出问题怎么查

| 现象 | 原因 / 处理 |
|---|---|
| 打不开，`docker compose ps` 显示 app 一直 restarting | `docker compose logs app` 看报错。多半是 `.env` 里的 key 填错格式 |
| 登录页能开，但一直提示用户名密码不对 | 初始管理员只在**首次启动、账号库为空**时创建。如果你改过 `ADMIN_PASSWORD` 后重启，密码不会跟着变——需要手动改：`docker compose exec app python -c "import sys;sys.path.insert(0,'/app');from app import auth;auth.set_password(1,'新密码')"` |
| 手机按麦克风没反应 | 必须走 HTTPS。检查地址栏是不是 `https://`（自签证书也算） |
| 语音很卡、断断续续 | 带宽不够，或者 Caddy 在缓冲。确认 `Caddyfile` 里有 `flush_interval -1` |
| 提示「今天的 AI 用量已达上限」 | 正常，说明配额在起作用。按第六节调高 |
| 传截图后一直「识别中」 | 视觉模型慢（一张图约 13 秒），等就行。超过 2 分钟看 `logs` 里有没有 API 报错 |
| 忘记管理员密码，也进不去 | `docker compose exec app python -c "import sys;sys.path.insert(0,'/app');from app import auth;auth.set_password(1,'新的强密码')"` |

---

## 十、这套配置的安全边界

已经做了：

- ✅ 登录门禁（签名 cookie，30 天有效）
- ✅ 密码 pbkdf2-hmac-sha256 + 每人独立 salt，120000 轮
- ✅ **数据物理隔离**：一人一个数据库文件
- ✅ API key 只存在服务端，前端拿不到
- ✅ 每人每日调用配额，防止共用 key 被烧穿
- ✅ HTTPS（备案前自签、备案后真证书）
- ✅ 安全响应头、关闭搜索引擎收录
- ✅ 容器日志限 10MB × 3，不会写满磁盘

**还没做的**（如果你要给别人长期用，建议补上）：

- ⚠️ **没有找回密码**（需要邮件服务，目前只能管理员手动重置）
- ⚠️ **没有登录失败次数限制**（暴力破解只能靠强密码挡）
- ⚠️ **开放注册期间**，任何知道网址的人都能注册（第六节有说明怎么关）
- ⚠️ 上传文件没有做病毒扫描（只校验了扩展名和大小）

对「自己和几个朋友用」这个场景，现在的强度是够的。
