# 求职助手 · 交付验收表

> 交付日期：2026-09-12　｜　验收人：（本人）
> 代码位置：`product/job_assistant/`
> 需求依据：`需求文档.md`（PRD v2.1）／`求职助手-产品需求文档.docx`
> UI 依据：`design/UI稿-还原/`（你给的完整 UI 稿，已落进实现）

---

## 一、怎么启动（3 条命令）

```sh
cd product/job_assistant

./run.sh            # 电脑：http://127.0.0.1:7870
./run.sh --https    # 手机：https://<本机IP>:8443（要开麦克风就必须用这条）
```

- 依赖已装在 `.venv/`，密钥已配好，**不用再装任何东西**。
- 新增依赖（读扫描版 PDF 用）：`pypdfium2`、`Pillow`；**流式语音用 `websockets`**。都已写进 `requirements.txt`。
- 手机第一次打开会提示「不安全」——自签证书的正常现象，点「继续访问」即可，之后麦克风才能用。
- 换电脑部署：`python3 -m venv .venv && .venv/bin/pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple`，再把 `.env.example` 复制成 `.env` 填 key。

---

## 二、六个原始需求 · 逐条对照

| # | 你的要求 | 完成度 | 怎么验收 |
|---|---|---|---|
| 1 | 完整理解我输入的项目内容 | ✅ 完成 | 「我的 → 材料库」传简历截图/项目文档 → 解析出事实；「我的 → 我的档案」逐条确认。实测一份简历截图读出 **34 条事实**，每条带出处 |
| 2 | 针对项目和招聘要求改出最吸引面试官的简历 | ✅ 完成 | 「简历 → 包装建议」选岗位后生成。实测 **15 条建议**，每条给「现在/建议/为什么」三段对比 + 依据，分三级 |
| 3 | 模拟面试，针对岗位和简历真实问答 | ✅ 完成 | 「面试 → 模拟面试」选岗位/简历/轮次/压力。实测追问链、五维评分、复盘建议都出得来 |
| 4 | 多模态，面对面谈话式面试 | ✅ 完成 | 通话页语音问答（TTS 朗读题目 + 你说答案 + ASR 识别）。实测 TTS 合成→ASR 识别**原句一字不差**。手机需走 `--https` |
| 5 | 记录所有面试内容并针对性给建议 | ✅ 完成 | 「面试 → 面试记录 → 复盘」：五维评分、评分趋势、最亮/最危险、改进计划、逐题拆解（含每轮追问原文） |
| 6 | 最终帮我拿到 offer，可按需扩展 | ✅ 已扩展 | 岗位匹配度分析（JD 四层拆解）、投递看板、题库、变更记录 |

---

## 三、功能清单 · 已完成 / 未完成

### ✅ 已完成并验证

| 模块 | 功能 | 验证方式 |
|---|---|---|
| 材料 | 截图 / PDF / **zip 源码包 / GitHub 仓库地址** → 自动选路解析成事实 | 截图走视觉（32 秒 34 条事实）；PDF 走文本层（**3.4 秒 23 条事实**）；扫描版 PDF 逐页转图走视觉（35 秒 33 条事实）；**真仓库实测：26/111 个文件 → 78 条事实，16 秒** |
| 项目总结 | 一键生成项目总结文档（定位/角色/架构/量化结果/难点/可能被追问的点）供预览 | 真调用 AI，Trade Master 生成 1571 字，材料里没有的会写「材料里没有说明」 |
| 档案 | 逐条确认 / 修改 / 标记失效 / 批量确认 | 接口测试 + 页面截图 |
| 项目库 | 项目要点确认与作废 | 页面截图 |
| 岗位 | 录入（粘贴原文或传 JD 截图）、JD 四层拆解（硬门槛/核心职责/加分项/隐性偏好）、匹配度打分、投递建议、缺口清单 | 真调用 AI，实测 72 分 + 逐条证据 |
| 简历包装 | 逐条建议 + 三级分级 + 依据；「需要补充」可直接填数字；应用修改到简历 | 真调用 AI，15 条建议（14/0/1） |
| **简历对话助手** | 对话式改简历：能读全部资料（档案/项目/材料/岗位/项目总结），可传图，**对话落库**（刷新能接着聊），草稿常驻可预览可保存 | 真调用 AI：一句话指出 3 个硬伤 + 列出 8 条要补的信息 + 生成 3495 字草稿 |
| 简历管理 | 新建 / 编辑 / 包装 / **删除**（删简历保留题库与面试记录） | 接口 + 页面实测 |
| 题库 | 分批生成、风险标注、状态管理（未练/已练/已掌握/待复习） | 真调用 AI，12 题 / 2 批 |
| 练习模式 | 一题一屏、可朗读、可语音或打字作答、**自己选是否先看答案**、看答案后给标准答案/考察点/想听的重点/追问链、左右滑动切题 | 接口实测 + 页面截图 |
| 模拟面试 | **像真人打电话的对话式面试**：开场白自然、顺着你的话追、话题自然过渡、可随时打断、音色可选、最多追 2 层、五维评分 | 真调用实测：接住具体用词、听得出回避、12 道题库不再被按序念完 |
| 复盘报告 | 五维条、评分趋势（对比同岗位上一次）、最亮/最危险、改进计划、逐题拆解；**没出评分的可以一键补** | 真调用 AI + 页面截图 |
| 语音 | TTS 朗读、ASR 识别、录音波形 | 真调用往返验证 |
| 投递看板 | KPI + 按状态分组 + 加入看板 | 页面截图 |
| 我的 | 材料库（三个分类）/ 项目库 / 投递看板 / 设置 / 变更记录。**「我的档案」已取消**——确认的单位是材料，不是逐条事实 | 页面截图 + 接口实测 |
| 响应式 | 一套 UI：375px 移动端（胶囊标签栏）/ ≥1024px 桌面端（侧边栏 + 双列） | 两个宽度都截图确认 |
| HTTPS | 自签证书（含本机 IP SAN）、手机可用麦克风 | 实测 8443 端口 + 证书 SAN 正确 |

### ⚠️ 未做 / 没验证到位（不含糊，逐条列）

| 项 | 状态 | 说明 |
|---|---|---|
| **真机浏览器录音** | 未验证 | 没有真手机，只验证了「HTTPS 服务正常 + getUserMedia 代码路径 + TTS/ASR 往返」。**需要你拿手机跑一次**：`./run.sh --https` → 手机开 `https://<IP>:8443` → 点「我先答」看能不能录 |
| 扫描版 PDF 默认只读前 8 页 | 有意限制（可调） | 扫描件要逐页转图调视觉模型，一页约 18 秒。默认上限 8 页，超了会在摘要里写明「共 N 页，只读了前 8 页」，**不会假装读全了**。要读全就在 `.env` 里调 `SCAN_MAX_PAGES` |
| 简历结构化编辑 | 只做了一半 | 按 PRD 的取舍，移动端只给「查看 + 改字 + 复制到剪贴板」，**没做**结构树/A4 预览；完整排版要在电脑上弄 |
| 投递看板转化率 | 数据口径单一 | 只统计「进面率」，没做「简历通过率／offer 率」的分母（样本量太小，算了也是假的） |
| 题库按岗位筛选 | 前端过滤 | 接口支持 `job_id`，但页面当前把所有岗位的题放一起分组展示，没做岗位下拉筛选 |
| 导出简历为 PDF/Word | 未做 | 目前是「复制全文」到剪贴板 |
| 多人多设备同步 | 未做 | 单机 SQLite，没做账号体系 |

---

## 四、自动化测试结果

### 单元 / 接口测试：**214 passed**

```sh
cd product/job_assistant && .venv/bin/python -m pytest
# 214 passed, 4 warnings in 7.65s
```

| 测试文件 | 条数 | 覆盖 |
|---|---|---|
| `test_core.py` | 26 | 配置三级回退、JSON 抢救、数据库 schema、状态流转 |
| `test_packaging.py` | 18 | 建议分级、**无依据强制降级**、应用修改 |
| `test_questions.py` | 14 | 分批生成、去重、截断抢救、自适应缩容、状态管理 |
| `test_interview.py` | 33 | 追问上限、五维评分、复盘、记录、**面试官要听得见回答**、**追问不能覆盖主回答**、**删简历后题库不能变孤儿**、**结束条件** |
| `test_api.py` | 35 | 60 个 REST 路由的参数校验与错误码，**含删除/新建类接口真的生效**、**岗位截图必须解析出原文**、**匹配度必须对着简历算** |
| `test_frontend.py` | 43 | 静态资源 200、ES Module 语法、设计令牌、`[hidden]` 兜底、**通话页不泄露答案**、**每个二级页面都有页内入口**、**页面调用的 api 方法必须都存在**、**入口与 import 必须同 URL（防模块执行两遍）** |
| `test_pdf.py` | 17 | 流解压、ToUnicode 映射、1/2 字节字形、**乱码闸门**、扫描件逐页渲染、只读前 N 页要标注 |

> AI 调用在测试里全部打桩，所以 2 秒跑完、不烧额度。

### 端到端（真调用 AI）：**6/6 步通过**

```sh
.venv/bin/python tests/e2e_pipeline.py
```

| 步骤 | 真实结果 | 耗时 |
|---|---|---|
| 1 材料解析（视觉读简历截图） | 34 条事实 | 33.5s |
| 2 岗位分析（JD 四层拆解 + 匹配度） | 72 分 + 投递结论 | 30.1s |
| 3 建立简历 | 从基线导入 12377 字 | 0.0s |
| 4 简历包装 | 15 条（14 可直接采纳 / 0 需补充 / 1 不建议写） | 19.2s |
| 5 题库生成 | 12 题 / 2 批，7 道风险题，**全部带标准答案和追问链** | 32.6s |
| 6 模拟面试（追问 + 五维评分） | 追问 2 次封顶，五维评分正常产出 | 7.7s |

### PDF 解析（真调用）：三条路都验过

| 文件 | 走哪条路 | 结果 | 耗时 |
|---|---|---|---|
| 简历截图 png | 视觉模型 | 34 条事实 | 32s |
| `项目发展现状.pdf`（有文本层） | 文本层 → DeepSeek | 23 条事实 | **3.4s** |
| `模拟交易AI托管App_PRD需求文档.pdf`（扫描版） | 逐页转图 → 视觉 | 33 条事实（2 页） | 36s |

### 语音链路（真调用）：TTS → ASR 往返

```
TTS  200  276524 bytes  (RIFF WAVE, 16bit 单声道 24kHz)
ASR  200  {"text":"我在同花顺负责闪电下单的市价委托功能，日活大概一千四百人。"}
```
**与输入原句完全一致。**

---

## 五、开发过程中修掉的真 bug

这些都是实际跑出来才暴露的，不是纸面问题：

| # | Bug | 症状 | 修法 |
|---|---|---|---|
| 1 | 题库生成撞输出上限 | 一次生成 12 题超出模型输出上限，**整批作废**、一题不剩 | 截断时抢救已完整的题目 + 自动缩容重试 |
| 2 | `run.sh` 变量被吞 | 启动横幅里的 IP 打成乱码 `��` | `$IP）` → `${IP}）`（bash 把全角字符首字节当成了变量名） |
| 3 | CSS `[hidden]` 失效 | `display:flex` 盖掉 UA 的 `display:none`，**返回箭头永远显示** | 加 `[hidden]{display:none!important}` |
| 4 | 练习模式统计不准 | 「是否先看了答案」永远记成 false，练习结束的统计恒为 0 | 按题跟踪 `peeked`，直接看答案也落库 |
| 5 | JD 分析报 500 | 分析失败时抛原始 `ValueError`，前端只看到「服务器错误」 | 收敛成 HTTP 400 + 可读原因 |
| 6 | 测试假绿 | 题库生成 0 题时 e2e 脚本仍报 ✅，核心功能坏了却全绿 | 0 题直接判失败 |
| 12 | **结束面试点了没反应** | 通话页 `z-index:70`，确认弹层 `z-index:60`——弹层被盖在全屏通话**下面**，用户看到的就是按钮死了 | 弹层提到 100、toast 110 |
| 13 | **面试官听不见回答** | 追问判断只喂了「问题 + 最后一句回答」，没给本轮追问链、也没给之前问过的题。候选人讲了一大段、核心点已经答到，面试官还在重复问「这个数据怎么统计的」 | 把本轮完整对话 + 之前问过的题一起喂进去，并改掉「没答到就一定要追问」的倾向 |
| 14 | **追问的答案覆盖了主问题的答案** | `interview_turn.answer` 只有一个字段，回答追问时也往里写。面试记录变成「问：日活怎么统计？答：换个手机会被算两次」，而且**评分模型读的就是这份错位记录** | 加 `is_followup` 区分：主回答写 `answer`，追问回答写 `followups[].a` |
| 15 | 答完直接蹦下一题 | 中间没有任何接茬，像在跟机器表单对话 | 追问判断同一次调用里多产出一个 `ack`（不额外花钱），前端先朗读再说下一题 |
| 7 | **PDF 文本层形同虚设** | 压缩过的 PDF 一个 `Tj` 都扫不到，所有 PDF 都被当扫描件丢给视觉模型 | 用 `zlib` 解压内容流 + 解析 ToUnicode 映射 |
| 8 | **PDF 抽出乱码却不报错** | 多字体 PDF 的字形编号会串，解出 6091 个"汉字"全是错位的，**静默写脏档案** | 加自然语言闸门：高频字占比 <3% 判为不可信，回退视觉 |
| 9 | 文本层被当成模型返回值 | 抽出来的原文直接喂给 `parse_json_loose`，必然 400 | 文本层也要再过一遍模型整理成 JSON |
| 10 | **视觉模型不接受 PDF** | 扫描件走 `image_url` 装 PDF，DashScope 直接报 `InvalidParameter: The image format is illegal` | 用 pypdfium2 逐页渲染成 JPEG 再送视觉 |
| 11 | 扫描件页码丢失 | 模型返回空 `locator` 时 `setdefault` 不生效，事实没有出处 | 改成判空再补页码 |

---

## 六、UI 稿校验结论

你的 UI 稿整体很完整（548 行 CSS + 13 个屏幕），我**按稿子重做了整套视觉**。校验时发现 4 个问题，实现时做了修正：

| # | 稿子里的问题 | 我的处理 |
|---|---|---|
| 1 | 每个屏幕顶部画了**假状态栏**（9:41 + 信号/WiFi/电池） | **去掉**。真机浏览器本来就有系统状态栏，再画一个既重复又会在别人的手机上显示错误时间 |
| 2 | 一级页面也带返回箭头（如 `practice`、`profile` 同时是标签栏目标） | **一级页面不给返回箭头**——标签栏能直达的页面没有"上一页" |
| 3 | `.chip-btn` / `.check-ok` / `.check-no` 用了但没定义 | 补齐样式（不然「复制」「重答」按钮和 ✓/✗ 标记是裸的） |
| 4 | 对比度不达标：`--muted` 在浅底只有 **2.38:1**、深色页次要文字只有 **3.15:1**（WCAG AA 要求 4.5:1） | 压深到 ≥4.5:1。手机在户外看这种灰字是看不清的 |

另外，稿子把标签栏的 5 个入口指向了二级页面（`practice`/`job-detail`/`resume-wrap`），这是还原稿的取巧；实现里保留了真正的一级入口（列表页），二级页面从列表进。

---

## 六点五、关于"改完看不到变化"（浏览器缓存）

第一次改完侧边栏你说没变化，查下来是**浏览器缓存**：静态资源没发 `Cache-Control`，
浏览器就按启发式缓存一直用旧 JS；而这个前端是 SPA，页面不刷新的话改完也永远跑旧代码。

已经治本：

- 所有静态资源加 `Cache-Control: no-cache, must-revalidate`（每次回源校验，没变就 304，不重传）
- 首页吐出的资源 URL 带构建时间：`app.js?v=0912115042`，文件一变 URL 就变，缓存自动绕开
- **侧边栏底部显示「构建 09-12 11:50:42」**——看到这行就说明新代码生效了，没看到就还是旧的

---

## 六点八、第三轮反馈的 7 个问题

| # | 你反馈的 | 根因 | 改法 |
|---|---|---|---|
| 1 | 点「有面试还没复盘」不跳转 | 后端给的 action 是 `#/records` / `#/profile` / `#/practice`——**路由表里没有这些**，前端静默回退到工作台，表现就是"点了没反应" | 改成真实路由（`#/interview/records` 等）；**未知路由不再静默**，会弹提示并打 console，这类 bug 藏不住了 |
| 2 | 从工作台进简历包装后没有返回入口 | 返回按钮在，但回退依赖内存里的 `backStack`——刷新过或从链接直接进来时栈是空的，回退会莫名跳工作台 | 加了层级表：栈空了就**回所属一级页**（简历包装→简历、面试任意二级→面试）；工作台「简历包装」快捷入口也直接进包装页 |
| 3 | 项目库删除没生效 | 状态确实改了（`obsolete`），但列表把**所有**要点都渲染出来，删掉的还在 | 已删除的不再显示，并注明「已删除 N 条（不再参与简历和面试）」；要点清空时给补数据的指引 |
| 4 | 数据总览太杂 | 原来堆了 8 个数字 | 只留两样：**岗位要求材料** / **简历·项目材料**。并明确告诉用户：传项目材料（GitHub 仓库 / zip 源码包）面试官才挖得动，只传简历它只能问简历上那几行字 |
| 5 | 简历页面只能加不能删 | 没做删除 | 加 `DELETE /api/resumes/{id}` + 列表页删除按钮（带确认）。级联规则：建议一起删，题库/面试记录保留（对岗位仍有效） |
| 6 | 简历页要有对话功能 | 没做 | 新增 `#/resume/chat`：**能读全部资料**（档案/项目/材料事实/岗位/现有简历），可传图（解析成事实后自动进上下文），产出草稿先预览、确认后才存进简历列表 |
| 7 | 点模拟面试提示没有题库 | 岗位和简历是两个**独立**下拉框，很容易选到"这个组合没题"的组合；而且没有任何提示或补救入口 | 选完立刻检查题库：有题就显示「这个组合有 N 道题可用」；没题就地给「生成题库」按钮；换岗位时自动切到绑该岗位的简历。练习模式同样处理 |

### 关于 zip / GitHub 项目材料

- 支持 `https://github.com/owner/repo`（走 codeload，主站取不到时也能用）和 zip 源码包
- 自动跳过 `node_modules` / `.git` / 构建产物 / 二进制 / 压缩包；README 与配置文件优先
- 上限 60 个文件 / 24000 字符，分批抽事实后去重合并
- 实测（真仓库 `Alex-original/trade_master`，260 KB）：读了 26/111 个文本文件 → **78 条事实，16 秒**

---

## 六点九九九、第十九轮：面试的结束条件（顺便修了「只能手动结束」）

### 你问的没错——之前**只能手动点结束**

查的时候发现结束闸门里引用了一个已经被我改名的变量（`used` → `used_idx`）：

```python
if action == 'finish' and followup_count < 2 and _remaining_topics(conn, interview, used):
#                                                        ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
#                                                        NameError: name 'used' is not defined
```

**只要模型说"聊完了"，请求就抛 NameError**。也就是说自动结束这条路从来没生效过，
面试只能靠你手动点「结束面试」。这是我上一轮改话题判重时留下的，抱歉。

修的时候又发现第二个坑：一句提问会把**所有**相似度超阈值的题都标成"已聊"。
题库里如果有几道长得很像，剩余话题瞬间清零 → 面试刚开场就结束。
改成**一句提问只认领最像的那一条**。

### 现在的结束条件（四条）

| # | 条件 | 说明 |
|---|---|---|
| 1 | **面试官判断聊完了** | 但它说"聊完了"要过闸门，见下 |
| 2 | **题库聊完了** | 没有未聊过的话题了 |
| 3 | **聊到 15 个话题** | 无条件强制收尾（`MAX_TOPICS`），不管模型还想不想问 |
| 4 | **你手动点「结束面试」** | 随时可以，结束就给评分 |

闸门（防止面试官过早收尾）：

- 聊过 **< 5 个话题**（`MIN_TOPICS`）且还有没聊过的 → **不放行**，继续问
- 还有没聊过的、且聊过不到 9 个 → 再补几个
- 否则放行

所以正常一场是 **6~9 个话题**，最多 15 个。

### 顺手加了进度显示

通话页现在会显示：

```
已聊 5 个话题（聊够 5 个左右面试官会收尾，也可以随时点结束）
```

省得你一边答一边猜"这到底还有几题"。

新增 4 条回归测试：模型说结束要真的能结束（不再 NameError）、
聊太少不许结束、相似题库不会被一次标光、到上限强制收尾。

---

## 六点九九八、第十八轮：记录粘在一起 / 清理不生效 / 返回跳回考场

### 1 · 记录粘在一起 —— 是我上一轮埋的

上一轮为了防"重复渲染"，我给每次渲染套了一层容器：

```js
const box = el('div', {});     // ← 没有类名
view.appendChild(box);
```

而所有间距、宽度、桌面双列栅格都挂在 `.page > *` 上。多了这层空 div 之后，
规则全部落空——记录卡片变成一条挨着一条。**顺带把工作台的桌面双列栅格也弄坏了**，
只是你没看到那一屏。

改法：`#view` 退成纯挂载点，每次渲染出来的容器承担 `.page` 的职责：

```html
<main id="view"></main>          <!-- 挂载点 -->
```
```js
const box = el('div', { class: 'page' });   <!-- 真正承载布局 -->
```

实测：记录卡片间距恢复 **14px**；工作台在 1280 宽下恢复双列。

### 2 · 清理不生效 —— 界面说的和实际删的不是一回事

| | 条件 |
|---|---|
| 界面判定「半途退出」 | `status='running'` 且没有评分 |
| 接口实际删除 | `status='running'` 且**一个字都没答过** |

你那 5 场都是「答过一点但没结束」，所以接口一条都不删，界面纹丝不动。
**界面的数字和接口的行为对不上**，这是我的锅。

改法：把两种情况分开说清楚——

- **一个字都没答过** → 「有点开就退出的面试 N 场」+ 清理按钮（数量与接口严格一致）
- **答过但没结束** → 「有答过但没结束的面试 N 场」+ 说明「点各条下面的补评分，
  约 10 秒出复盘。删掉就可惜了，所以这里给的是补评分而不是清理」

列表接口加了 `answered_turns`，界面才能自己分清这两种。原来的黄色提示块和蓝块
说的是同一批记录，合并成一块了。

### 3 · 复盘页点返回，又回到模拟面试页

面试结束时是 `navigate(..., {replace: true})` 跳到复盘页的，
所以浏览历史里复盘页的**前一步是"模拟面试设置页"**。点返回 → `history.back()`
→ 又回到考场门口。

改法：**返回按层级走，不按浏览历史走**。复盘页的上一级就是面试记录：

```js
if (param === 'report') return '#/interview/records';
```

结束面试时顺手清空返回栈。实测：模拟面试设置页 → 复盘页 → 点返回 → **面试记录** ✅

新增 5 条回归测试：`.page` 必须落在渲染容器上、返回层级表要有复盘→记录、
结束面试要清返回栈、清理数量必须和统计一致、列表要给出 `answered_turns`。

---

## 六点九九八、第十七轮：侧边栏页脚的数字不对

### 你说得对，而且底下还藏着一个更麻烦的

**问题 1：数字用的是已经作废的口径。**
页脚显示的是 `fields_unconfirmed`（profile_field 里没确认的字段数 = 121）。
「我的档案」模块上一轮已经取消，这 121 条既没有意义、也没有任何地方能处理它。
——上一轮我改了工作台卡片的口径，**页脚漏改了**。

**问题 2：「待复盘」永远清不掉。**
`reviewed_at` 这个字段**从来没有被写过**——查遍了所有代码，只有读、没有写。
所以哪怕你把每一场的复盘报告都看完了，「待复盘 17」也只会越涨越多。
用户看完了，系统不知道他看过了。

### 改法

1. 页脚不再显示那两个来路不明的数字，改成**列真实待办**，每条都能点进去处理：
   ```
   待办 17
   面试待复盘        17
   ```
   （待办为空时显示「没有待办」）
2. 新增 `POST /api/interviews/{id}/reviewed`；**打开复盘页就等于复盘过**，
   进页面时自动记一笔。实测标记一场后「待复盘」从 17 降到 16，重复标记幂等。
3. 每条待办加了短标签（材料待核对 / 简历建议待处理 / 题目待复习 / 面试待复盘），
   侧边栏和待办清单共用。

新增 3 条回归测试，反向验证过：把 `reviewed_at` 的写入去掉，测试立刻报
「待复盘清不掉」。

---

## 六点九九七、第十六轮：面试官重复提问 + 复盘加「标准回答案例」

### 一、重复提问：根因是判重方式错了

复现出来很清楚（同一场面试里）：

```
Q1: 你简历里说重构交易查询模块，复用率达到90%，这个90%具体是怎么算出来的？
Q3: 好，那我问个别的——你说重构交易查询页面用了代理模式和工厂模式解耦，
    复用率达到90%，这个90%具体是怎么算出来的     ← 同一件事，换了个说法
Q4: 你简历里写用 Claude 把 iOS 代码转成 Android 代码、7000 行，这个转换具体是怎…
Q5: 好，那换个话题——你简历里写用 Claude 把 iOS 代码转成 Android 代码、7000 行…  ← 又一遍
```

代码是这样的：

```python
used = {r['question'] for r in history_rows}          # 模型问出来的原话
topics = f'[{"已聊" if q["question"] in used else "未聊"}] {q["question"]}'   # 题库原文
```

**模型每次都会"用自己的话"问**（这是上一轮我特意要求的，为了不像念题库），
于是它问出来的句子和题库原文永远对不上 → 题库里每一条永远显示「未聊」→ 反复问同一题。
上一轮的两个改动互相打架，这是我的疏忽。

### 改法：按话题编号记账 + 按「像不像」兜底

1. 题库带编号给模型（`[0] … [1] …`），让它在返回里说明问的是第几条
2. 新加 `interview_turn.topic_index` 记住编号，判重看编号不看文字
3. 模型有一半时候不返回编号，所以补一条**相似度兜底**：中文按字符 2-gram 算重合度

阈值是量出来的，不是拍脑袋：

| 类型 | 重合度 |
|---|---|
| 同一件事（复用率 90% 口径） | 40% |
| 同一件事（7000 行转换） | 89% |
| 相关但不同（90% vs 重构价值） | 20% |
| 完全不同 | 1% / 6% |

取 **30%** 能把"同一件事"和"相关但不同"干净分开。

4. 提示词里加了明确的反重复条款，并给了反例：
   「前面问过『90% 怎么算出来的』，后面又问『复用率 90% 的口径是什么』→ 同一件事」

复测（同一场，连问 5 题）：**Q1 iOS→Android 转换 / Q2 复用率口径 / Q3 Wind MCP / Q4 慢计划快执行 / Q5 68 vs 228 回归** —— 五件不同的事，重复 0 对。

### 二、逐题拆解加了「标准回答案例」

原来每道题只有「问题 + 你的回答 + 追问」。现在评审会逐题给出：

| 字段 | 内容 |
|---|---|
| 面试官想听什么 | 这道题真正在考什么（一两句） |
| 差在哪 | 没答到 / 没数字 / 没过程 / 自相矛盾 |
| **标准回答案例** | 用**他自己的项目和数字**组织一段可以直接背的答案，150-300 字 |

这道题如果本来就出自题库，匹配不到模型写的版本时退回题库原文（标注「题库原文」）。
面试官临场问的题（不在题库里）会明确说「这道题没有匹配到标准答案」。

实测第 1 题的「标准回答案例」：

> 我主力是 iOS，但后端不是零基础。Trade Master 是我独立做的全栈项目，用 Python
> FastAPI + PostgreSQL 写了 8.7k 行，Docker + nginx + HTTPS 部署在阿里云 ECS 上，
> 跑通了 600519.SS 的多智能体决策全链路……Java 目前是能读能改，我给自己定的补齐
> 路径是：用 Spring Boot + MyBatis + Redis 把同花顺那套交易查询重构用 Java 重写
> 一遍……4 周内放到 GitHub。

关键在提示词里写死了：**不许编造他没有的经历或数字**，资料里缺的地方要写成
「（这里需要你补一个真实数字）」。

新增 6 条回归测试：相似度阈值能分开同/异、换了说法也要对回编号、
话题清单要标已聊、反重复条款要在提示词里、评审要逐题给、题库原文要能兜底。

---

## 六点九九六、第十五轮：工作台的假指标

### 查出来三个「不真」的

| 指标 | 显示 | 真实情况 | 问题 |
|---|---|---|---|
| 档案完成度 | 33%（53/160） | — | 「我的档案」模块上一轮已经取消，这个指标没有意义，也没地方可去 |
| **已掌握题目** | **26/26** | **26 道全是"未练"，已掌握 0 道** | 用的是 `总数 - 待复习数`，把"没标待复习"当成了"已掌握" |
| 面试场次 | 35 场 | 真正答过内容的只有 14 场 | 21 场是只听了个开场就退出的，堆在一起把数字撑虚 |
| 待办「档案里有待确认的内容」 | 121 条 | — | action 指向 `#/mine/profile`，那个路由现在会跳到材料库，点进去驴唇不对马嘴 |

### 改法

**1. 删掉「档案完成度」** —— 模块都取消了，留着这个百分比只会误导。

**2. 「已掌握」老老实实数** `status='mastered'`，不再拿 `总数 - 待复习` 充数。
现在显示「0 ／ 共 26 题」——这才是真的：你确实一道都还没练。

**3. 面试口径拆成三个**：
- `interviews_finished` —— 真答过的（工作台显示这个）
- `interviews_abandoned` —— 只看了开场就退出的
- `interviews_unreviewed` —— 待复盘

**4. 待办换成「有材料的解析还没核对」** —— 档案取消后，确认的单位是材料。
指向材料库，点进去就能处理。

**5. 顺手做了个清理入口**（记录页）：检测到半途退出的面试时，出现
「清理这 N 场」按钮。**只删一个字都没答过的**，答过内容的记录一律保留——
清不清由你决定，我不替你删数据。

### 现在工作台显示的四项（都验过）

```
目标岗位    2
简历        3
已掌握题目  0 ／ 共 26 题
已面试      14 ／ 14 场待复盘
```

新增 4 条回归测试：已掌握必须数真实状态、面试场次排除半途退出、
清理只动没答过的、待办不能再指向已取消的模块。

---

## 六点九九九五、第十四轮：对话助手读的是哪份简历

### 你猜对了——它没读简历库里的任何一份

前端一直写死传 `resume_id: null`，于是 `build_context` 里那个「当前正在改的简历」分支
**永远不执行**。助手能看到的只有材料事实（也就是最早那张简历截图的解析结果），
所以怎么改都像在改最开始那一份。

### 改法

| | 之前 | 现在 |
|---|---|---|
| 选简历 | 没有，写死 null | 对话页顶部「**在改**」下拉：列出简历库里的每一份 |
| 上下文 | 不含任何简历 | **选中的那份全文**，并注明「★ 这次要改的简历：X（更新于 …）」 |
| 还记得有哪些别的 | 不知道 | 上下文里列出简历库其他版本，用户说"换一份"时它能问换成哪份 |
| 选择会不会丢 | — | 记在会话上（`chat_session.resume_id`），刷新还在 |
| 换简历 | — | 换了立刻明说一句「接下来按「X」改」 |
| 保存 | 只能存成新的 | 可以**覆盖当前那份**，也可以另存为新简历 |

实测：切到简历 #6 后问「第一段写的什么」，它答的是那份简历里真实的第一段
（「AI 工程实践，写你在 Android 团队用 Claude 把 iOS 代码转成 Android 交付，7000 行」），
不再是从材料事实里拼出来的。

新增 4 条回归测试：选中简历要进上下文、会话要记住选择、换简历要更新、要列出其他版本。

---

## 六点九九九、第十三轮：「未关联岗位」是什么意思

### 含义

`resume.job_id` 表示"这份简历是为哪个岗位定制的"。为空 = 通用简历，没指定投哪儿。
之前只显示一句「未关联岗位」，既没说清有什么用，也没有地方能改。

### 改法

| | 之前 | 现在 |
|---|---|---|
| 文案 | 「未关联岗位」 | 「目标岗位：支付宝 · AI工程师-金融智能」/「通用简历（未指定目标岗位）」 |
| 解释 | 无 | 未指定时补一句：指定后包装建议直接对着那份 JD 提，题库也按它出 |
| 能不能改 | 不能 | 「指定目标岗位 / 换目标岗位」按钮，弹出岗位列表可选；也能取消关联 |
| 有没有用 | 只是个标签 | **包装建议会自动用它**：进包装页不再重复问一遍选哪个岗位 |
| 布局 | 四个按钮挤一行，「包装建议」折行 | 改成两行两列 |

接口侧：`PATCH /api/resumes/{id}` 支持 `job_id`。这里有个细节——用
`model_fields_set` 区分「传了 null」（取消关联）和「没传」（不动它），
不然取消关联做不到。传一个不存在的岗位会返回 400。

---

## 六点九九八、第十二轮：取消「我的档案」模块

### 你说得对，那个模块是多余的

数据很能说明问题：**160 条字段，121 条待确认**——而它们全都是从材料里自动抽出来的。
等于让用户对着自己刚传上去的简历，一条条点 121 次「确认」。没人会这么干，这个模块就是白占地方。

而且更糟：那 121 条**没确认的事实，AI 是看不见的**（事实库只收已确认的），
所以用户不点那 121 次，AI 能用的信息反而更少。

### 改法：确认的单位从「一条事实」变成「一份材料」

| | 之前 | 现在 |
|---|---|---|
| 入口 | 「我的 → 我的档案」，160 条逐条确认 | **模块取消**，侧边栏和菜单里都没有了 |
| 确认粒度 | 一条事实点一次（121 次） | **一份材料点一次**：「确认这批事实（121 条）」 |
| 确认之后 | 事实提升进档案字段（同一件事存两遍） | 材料标记为「已核对」，事实直接进事实库 |
| 事实库 | 只收已确认的 profile_field | **材料事实为主体**：已核对的直接用；没核对的也带上，但**标明「用户还没核对过——用到这些数字前先跟他确认」** |
| 老链接 | — | `#/mine/profile` 自动跳到材料库，不让用户撞见空页面 |

关键是最后一条：**AI 现在能看到全部事实**，只是知道哪些还没核对过。这样既不用点 121 次，
也不会拿没核对的内容当铁证——它会在用到之前问你。这正好对应你说的「有需要确认的可以问我」。

### 顺带清掉的东西

- 「材料抽取」那段 121 条重复字段不再进事实库（同一件事说两遍）
- 死代码 239 行（`pageProfile` / `renderField`）+ 7 个没人调的 API 客户端方法
- 中途手滑把「项目库」页面一起删了，已经补回来（前端测试当场报出来了：`api.projects` 没人用）

---

## 六点九九七、第十一轮：岗位分析没有用我最新建的简历

### 根因

`analyze` 只把**档案事实库**（已确认的档案字段 + 项目要点）喂给模型，
**简历正文从头到尾没进过上下文**。所以你在简历里新写的东西，匹配度分析根本看不见——
新建简历再点分析，分数和结论几乎不变，表现就是"没用我的简历"。

### 改法

| | 之前 | 现在 |
|---|---|---|
| 分析依据 | 只有档案事实库 | 档案事实库 + **简历正文**（默认最近更新的那份，最多 12000 字） |
| 证据来源 | 只从事实库找 | 优先引用**简历里的原话**，事实库用来核对 |
| 能看出矛盾 | 不能 | 简历里写了但事实库没依据的，会标出来（面试会被追问） |
| 用了哪份简历 | 不记录、不显示 | 记录 `analyzed_resume_id`，详情页显示「上次分析用的是「X」（更新于 …）」 |
| 换简历 | 不能 | 分析前可以**自己选**对着哪份简历算 |

实测（同一个岗位 #9，只是把简历加进上下文）：

```
改之前：58 分，14 条要求
改之后：62 分，23 条要求（硬门槛 5 / 核心职责 8 / 加分项 4 / 隐性偏好 6）
        用了简历 = '最新简历 2026/9/12'（3878 字）
```

新增 4 条回归测试，反向验证过：把简历注入关掉，立刻报「简历内容没进分析上下文」。

---

## 六点九九五、第十轮：上传岗位截图没有正确解析岗位要求

### 两个 bug 叠在一起

**① `create_job` 从来不解析 JD 截图。** 界面上写着「保存后会一起解析 JD 内容」，
实际上后端只把 `material_id` 存下来就完事了——`job.raw_text` 是空的。
后果是岗位详情页没有要求、匹配度分析拿不到岗位要求（报「没有 JD 原文」，或者只拿一句摘要充数）。

**② 通用材料解析不保存原文。** `parse_material` 只存 `summary`（一句话摘要）和若干事实，
`material.raw_text` 只在解析 zip 时才写。所以就算解析过，岗位分析拿到的也只是
「这是一份招聘 JD…」这种摘要，不是真正的要求。

### 改法

| | 之前 | 现在 |
|---|---|---|
| 上传 JD 截图 | 只存文件，不解析 | **上传完立刻解析**，并把识别出的原文填进「岗位要求原文」输入框 |
| 解析内容 | 一句话摘要 + 零散事实 | **逐字转录的 JD 全文** + 公司/岗位/城市/薪资 + 职责与要求逐条 |
| 岗位原文 | 空 | 483 字的真实 JD 原文（实测那个蚂蚁「AI Agent 应用产品」的截图） |
| 核对 | 无从核对 | 页面上把识别结果整段列出来，让用户一眼看出漏了哪条、认错了哪个字 |
| 兜底 | 无 | 即使用户直接调接口建岗位，后端发现材料没解析过也会**自动补解析**；已解析过的不重复花那 13 秒 |

实测（真调用视觉模型，13.5 秒）：

```
公司     = '支付宝（杭州）数字服务技术有限公司'
岗位     = '【AI Agent】应用产品'
城市     = '杭州'
原文     = 483 字
要求条数 = 7
── 原文节选 ──
岗位职责：1. 洞察用户在对话/多模态交互/LLM Agent领域的应用场景和需求类型…
任职要求：1. 具备大模型Agent或AI产品的实践经验，熟悉主流大模型技术…
```

建岗位 → 匹配度分析：**62 分，拆出 14 条要求**（硬门槛 3 / 核心职责 4 / 加分项 2 / 隐性偏好 5）。
改之前这一步是直接报错的。

新增 4 条回归测试，反向验证过：把自动解析关掉，其中 2 条立刻失败。

---

## 七、第九轮：语音太生硬，要更低延迟

### 先量了一下，瓶颈很清楚

| 环节 | 实测耗时 |
|---|---|
| TTS 整段合成（59 字的问题） | **3.02s** |
| ↳ 下载那段音频 | 0.23s |
| ASR 识别（11 秒录音） | 0.89s |

也就是说：**等 3.25 秒才听到第一个字**，而下载只占 0.23 秒——瓶颈全在"整段合成完才返回"。
这就是"生硬"的物理来源：真人接话是 0.2~0.5 秒，你这边是 3 秒。

### 换成流式合成：首字节 0.45s（快 5 倍）

DashScope 有 realtime WebSocket 接口（`qwen3-tts-flash-realtime`），**边生成边吐 PCM**。
新增 `POST /api/voice/tts-stream`：服务端把 WS 收到的音频块直接转发给浏览器，
前端用 Web Audio 边收边排进播放队列。

| | 老的整段接口 | 新的流式接口 |
|---|---|---|
| 首字节 | 2.20s | **0.42s** |
| 全部完成 | 2.20s | 3.03s（但用户早就在听了） |

浏览器里实测（真实点开通话页）：**第一块音频 0.41s**，整句 2.38s 播完。
前端还做了兜底：流式走不通就自动退回老的整段接口，不会整个哑掉。

### 顺带解决的三件事

1. **静音自动提交（VAD）**：说完停顿 1.1 秒自动结束录音并提交。
   真人对话不会"说完再点一下按钮"——这一步省掉，接话感立刻好一半
2. **音频上下文预热**：浏览器要求音频必须由用户手势启动。
   现在在「开始面试」「点这里回答」「试听」三个点击里先唤醒 AudioContext，
   不然 `resume()` 会一直挂着（headless 里踩到过：一个请求都发不出去）
3. **播放结束判定加兜底**：`onended` 在没音频设备的环境不触发，
   只靠它会把整场对话卡死。现在按"排好的播放时刻"兜底 resolve

### 还能更快吗

再往下就是 **ASR 也走流式**（边说边出字，说完整句立刻提交，省掉 0.9s 的收尾识别），
以及**全双工打断**（不用按按钮，靠 VAD 自动检测你在说话就停下）。
这两步需要把 ASR 也换成 WebSocket 长连接，改动比这次大，**还没做**——
现在的方案已经能到"你说完停顿一下，约 1.5~2.5 秒听到下一句"。

---

## 六点九九、第八轮：面试官要像真人对话，不要遍历题库

### 问题出在设计上，不是出在提示词上

原来的流程是：从题库取第 1 题 → 答完判断"追问还是下一题" → **下一题 = 题库里还没问过的那一行**。
所以不管你说什么，它都会按顺序把 12 道题念完——这就是"遍历题库"。

### 改法：题库降级成「弹药库」，下一步由面试官决定

| | 之前 | 现在 |
|---|---|---|
| 题库的角色 | **流程表**：按行念 | **弹药库**：可问的话题清单，标了哪些已聊过 |
| 下一题从哪来 | 取题库下一行 | **面试官决定**：顺着你刚说的话挖 / 挑没聊过的话题 / 结束 |
| 问法 | 念题库原文 | 用自己的话问，但保留关键数字和技术点 |
| 话题切换 | 无 | 自然过渡：「那咱们换个话题…」「那回到你那个视频笔记项目…」 |
| 开场 | 「第一题：……」 | 先打招呼再切入：「你好，我是这次的技术面试官，咱们直接开始。」+ 直接问简历里最该问的 |
| 追问上限 | 代码强制 2 层 | 不变（代码强制）。但触发上限时**丢弃模型给的追问话术**，改从题库挑新话题，避免突兀 |

两条底线仍然由**代码**保证，不靠模型自觉：一次只问一个问题；每个话题最多追 2 层。

### 实测一轮真实面试

```
开场：你好，我是这次的技术面试官，咱们直接开始。
第一问：你简历上写熟悉 LangChain、LangGraph，但项目里 LangGraph 好像只有讨论
        没有实际代码，你为什么用手动消息列表实现而不用 LangGraph？

我：Trade Master 是我独立做的，用 LangGraph 把基本面/技术面/风控三个 Agent 串起来…
  （嗯，那我理解。）
  追问：你说数据层从 yfinance 换成了 Wind MCP，具体怎么换的？那 7 个服务、
        39 个工具你是怎么接到 LangGraph 里的？
我：慢计划快执行是说深度分析要十几分钟，但执行压到 500ms 以内…
  （嗯，明白了。）
  → 换话题：那你从同花顺做了三年 iOS，现在转投 AI 方向，最大的劣势是什么？
我：对，75 条端到端用例都是我写的…
  （嗯，我明白。）
  追问：你刚才提到 75 条用例，那我先问——从 iOS 转 AI 最大的劣势到底是什么？
        你好像没正面回答。
```

它会**接住你的具体用词**（"你说数据层换成了 Wind MCP…"）、**听出你在回避**（"你好像没正面回答"）、**自然换话题**。

### 通话体验也一起改了

- **可以打断面试官**：他还在说话时按麦克风，立刻掐掉他的话让你开口。之前会弹"稍等一下"，很出戏（真人打电话本来就能插话）
- **开场白会念出来**，不再一上来就蹦第一题
- 麦克风全程可用（之前说话时会禁用）

---

## 六点九八、第七轮反馈的 4 个问题

| # | 你反馈的 | 根因 | 改法 |
|---|---|---|---|
| 1 | 岗位要能删除 | 没做 | 加 `DELETE /api/jobs/{id}` + 列表页和详情页各一个删除按钮。岗位要求、匹配度、投递与面试记录一起删；**题库和简历保留**（只解除关联），删除会告诉用户保留了多少 |
| 2 | 材料库要按岗位/简历/项目分开存 | 两个页签**根本不过滤列表**——点了还是全都显示，所以看着就是"没分开" | 改成三个分类页签（岗位材料 / 简历材料 / 项目材料），**真正按类过滤**；页签上带数量；上传时就把分类定下来（不等模型猜）；每个分类有各自的说明和上传提示；GitHub 地址输入框只在项目材料页出现 |
| 3 | 为什么有两个「模拟面试」按钮 | ★ 见第 4 条，同一个根因 | 修好后只剩一个 |
| 4 | 模拟面试还是有两条音轨 | ★★ **真因找到了**：`index.html` 里的入口是 `app.js?v=构建时间`，而页面模块写的是 `import ... from '../app.js'`（不带参数）。**ES Module 按 URL 去重**，这两个被当成两个不同模块各执行一遍 → 两份模块状态、两条渲染管线 → 页面内容出现两份（两个按钮）、通话页**同时请求两次语音合成**（两条音轨念同一道题） | ① 去掉 `?v=`，防缓存改用响应头 `no-cache, must-revalidate`（浏览器每次回源校验，变了取新的、没变走 304）；② 顺手把渲染做成隔离容器 + 同一路由不重复渲染；③ 通话页加实例守卫，被取代的实例不再说话 |

### 第 4 条是怎么查出来的

不是猜的，是加了一行栈追踪后打出来的：

```
speak() 被调用 2 次：
  #1 at pageCall (interview.js:978)
  #2 at pageCall (interview.js:978)
render #1  at app.js:201          ← 没有 ?v=
render #2  at app.js?v=0912154908:201   ← 带 ?v=  → 两个模块实例
```

服务器侧的 TTS 请求数也印证了：修之前 **2 次**，修之后 **1 次**。

这个坑是我自己挖的：上一轮为了治"改了看不到变化"加了 `?v=`，反而制造了更隐蔽的问题（同一份代码执行两遍）。现在防缓存只用响应头，并且加了**回归测试**：`index.html` 和所有 `import` 里都不允许出现带查询参数的模块路径。反向验证过——故意加回 `?v=`，测试立刻失败。

---

## 六点九七、第六轮：拉取 GitHub 仓库失败

**根因是我自己的低级错误**：给 `api.js` 加 `materialFromUrl` 时，替换用的锚点没匹配上
（当时写的是 `uploadMaterial: (file) => {`，实际是 `uploadMaterial: (file, projectId) => {`），
而脚本没做校验、**照样打印了"已加"**。结果就是按钮点下去报
`api.materialFromUrl is not a function`。同一个批次里 `projectSummary` 也一起漏了，
所以「生成项目总结」按钮同样是坏的。

**已经这样兜底**：

1. 两个方法都补上了，并且**用断言校验真的写进去了**（不再无条件打印成功）
2. 加了一条**自动检查**：扫 `web/js/pages/*.js` 里所有 `api.xxx()` 调用，
   逐个对 `api.js` 的定义，缺一个就测试失败。反向也查（定义了但没人用的，多半是改了一半）
3. 反向验证过：故意删掉 `projectSummary`，测试立刻报
   「api.projectSummary 没定义（mine.js 在用）」

实测（浏览器里真点按钮）：`拉取仓库` → **已拉取 254 KB** → **解析完成：85 条事实**。

---

## 六点九五、第五轮反馈的 4 个问题

| # | 你反馈的 | 根因 | 改法 |
|---|---|---|---|
| 1 | 对话助手要能**帮我完善/包装简历**，让我看看对话日志 | 两个问题：①**对话根本没落库**（只在浏览器内存里，刷新就没，我也查不到）；②提示词把它定位成"问答助手"，它会点评但不会主动产出简历 | ① 新建 `chat_session` / `chat_message` 两张表，每轮都存，**刷新页面能接着聊，也能事后查**；② 重新定位成「**简历教练**」：交付物是一份能直接投的简历，每轮都要推进草稿；③ 顶部常驻「当前草稿 · 3496 字 [预览] [保存]」；④ 加常用指令：`生成完整简历` / `我还缺什么` |
| 2 | 上下文限制放宽，不要截断 | 资料上限 40000 字符、历史只留 16 条 | 实测 deepseek-chat 吃 **12 万中文字符**没问题。改成：资料 70000 字符 + 历史 20000 字符（按字符预算从最近往回装，不再按条数砍），输出 8000 token |
| 3 | 不知道选的岗位有没有生成过面试题 | ★ 底下还有个真 bug：**删掉简历后，那个岗位的题库变成了孤儿**。题的 `resume_id` 被 `ON DELETE SET NULL` 置空，而取题时严格按 `resume_id` 匹配 → 明明有 12 道题却报"这个岗位还没有题库" | ① 取题改成「这份简历的题 **或** 岗位级的题」；② 岗位下拉里直接写题量（`… · 12 题` / `… · 还没题库`）；③ 选完显示绿色「题库已就绪 · 12 题，其中 7 道是风险点」；④ 没题就地给生成按钮 |
| 4 | 两个声轨重复提问 + 要按两次结束 | ★ **通话页会叠层**：只有"正常结束面试"才清理那层全屏遮罩，用户按浏览器返回、或改 hash 离开时它**永远留在 DOM 里**，再开一次面试就有两层通话页，两层各自在念题 = 两个声轨。另外 `speak()` 每次新建 Audio 又不管上一句，`play()` 只等到"开始播"就返回，所以追问和下一题会叠着播 | ① 路由切换时**统一清理残留的通话页**（`render()` 里收掉），通话页自己再加一道重复初始化保护；② `speak()` 播之前先掐掉上一条，并**等真正播完**才返回；③ 每次提问加令牌，只有最新那次能念；④ ack 改成只显示不朗读（它和下一题本来就会撞）。「结束」是两次点击：结束 → 确认，这是有意保留的（防止误触丢掉整场面试），确认按钮文案已改成动作明确的样式 |

> 第 4 条我实测验证过：进通话页 1 层 → 改 hash 离开后 0 层（之前会残留）→ 再进还是 1 层。

---

## 六点九、第四轮反馈的 8 个问题

| # | 你反馈的 | 根因 | 改法 |
|---|---|---|---|
| 1 | 待办「简历有未处理的修改建议」跳过去没有返回入口 | 跳到的是简历**列表**（一级页，本来就没有返回箭头） | 改成直达**那份简历的包装页**（有返回）；另外一级页面只要是"从应用内走进来的"（回退栈非空）也给返回箭头，工作台除外 |
| 2 | 我的档案要支持图片识别 + 删除 | 图片识别只放在材料库里；字段只能标失效不能删 | 档案页加「📷 从图片识别信息」，传图→解析→勾选加入档案，不用绕去材料库；每条字段加「删除」 |
| 3 | 项目要点里的「确认」是干嘛的 | 它**有用**（确认过的要点才会进简历和面试出题），但界面上看不出来，而且已确认的也显示 | 按钮改叫「确认可用」，已确认的改显示绿色「已确认可用」标签；项目卡片顶部写明这句话的作用 |
| 4 | 项目库要能删除和添加 | 两个都没有 | 加「＋ 新建项目」（名称/角色/时间/简介）和「删除项目」；删项目会删要点，**材料保留**（原始证据不能删，只解除关联） |
| 5 | 材料库要能删除 | 没做 | 加 `DELETE /api/materials/{id}` + 删除按钮，连带它抽出的事实一起删（会先提示条数） |
| 6 | 对话超过两轮就没反应 | ★ 真因：模型有时顺手把整份简历贴一遍，输出撞上长度上限被截断，解析不出来 → 返回空回复；而前端写的是 `if (out.reply)`，**空回复就什么都不显示** | 后端：改用能抢救截断输出的 `chat_json` + 空回复时用「只回一句」重试一次 + 真不行也返回一句人话；提示词明确「默认不输出全文」。前端：空回复也必须说话。实测 6 轮 0 次空回复 |
| 7 | 输入框回车没反应 | 绑的是 `Cmd/Ctrl + Enter`——手机上没这个键，桌面上也没人猜得到 | 改成 **Enter 直接发送**，Shift+Enter 换行 |
| 8 | 输入框和按钮高度不一致 | 输入框 44px、按钮 44px 但用 `align-items:flex-end` + 各自的 padding，视觉上错位 | 统一 44px、统一圆角 12px；输入框随内容长高（最多 4 行），长高后按钮仍贴底对齐；传图按钮改成 44×44 方形图标按钮 |

> 关于第 6 条你提到的「参考附件里的对话助手」——**那个附件没收到**。
> 现有的 `qiuzhi-zhushou.zip` 里只有 13 个屏幕（练习/通话/评分/工作台/岗位/包装/复盘/档案/材料/题库/看板），
> 没有对话助手那一屏。这次是照你设计稿既有的设计语言做的（卡片圆角 16、气泡、主色 #3D6BFF）。
> 想按特定稿子来，把图再发我一次。

---

## 七、第二轮反馈的 8 个问题

| # | 你反馈的 | 根因 | 改法 |
|---|---|---|---|
| 1 | 点「点这里回答」没变化 | 只有状态行变文字，按钮本身没反馈 | 按钮加红色脉冲环，标签变「录音中 00:12 · 点一下结束」，状态行变「● 正在听你说…」 |
| 2 | 点完按钮面试官的问题消失了 | 代码里写了 `summary.hidden = true` | 问题常驻在气泡里，录音时只压暗不隐藏；识别出的回答另起一个「你说的」气泡 |
| 3 | 答完没有反馈直接跳下一题 | 答完立刻喊下一题 | 模型先给一句 `ack`（「好，口径清楚了」），前端朗读完再接下一题 |
| 4 | 讲了一大段面试官像没听懂 | 追问判断看不到本轮对话历史和已问过的题 | 完整对话链 + 历史题一起喂；并明确「他用别的说法答到了就算答到」 |
| 5 | 点结束没反应 | 确认弹层被全屏通话页盖住（z-index 低了） | 弹层 z-index 提到通话页之上 |
| 6 | 最新记录只有对话没有复盘 | 那场面试状态卡在 `running`（因为 #5 结束不了） | 记录页和报告页都加「补评分」；这条记录已补出 23/50 |
| 7 | 想要能选音色 | 只有一个默认音色 | 模拟面试设置页加音色下拉 + 试听，选完记住；四个官方音色 Cherry / Serena / Ethan / Chelsie |
| 8 | 侧边栏一级二级混在一起 | 14 个页面平铺成一组 | **导航只留 5 个一级入口**；二级页面只从它自己的一级页进（唯一入口），导航里不重复放 |

---

## 八、我建议你明天重点验的 3 件事（补充：公开仓库已做脱敏）

1. **手机语音** —— 这是唯一没法替你验的（需要真机）：`./run.sh --https` → 手机访问 → 「面试 → 模拟面试」走一轮完整通话。
2. **简历包装的建议质量** —— 这是产品的核心差异点。看「需要补充」那一类是不是**真的在向你要事实**，而不是替你编数字（编了会在面试被击穿）。
3. **模拟面试的追问力度** —— 故意含糊回答一次，看它会不会追到你答不出来（会追 2 次然后放过）。

### 补充：提交到公开仓库前的脱敏处理

这个仓库是公开的（`github.com/flingjie/Agent-100-Days`），而 `product/` 不在 `.gitignore` 里。
交付前我扫了一遍，把会泄露个人信息的地方换成了占位示例：

| 位置 | 原来 | 现在 |
|---|---|---|
| `需求文档.md` 6.1 字段示例 | 真实手机号 | `138****0000` |
| `需求文档.md` 线框图 | 真实姓名 / 学校 | 「你的名字」/「某某大学（211）」 |
| `web/js/pages/dashboard.js` | 写死的问候姓名 | **改成从档案的「姓名」字段读取**（没有就不带名字） |
| `tests/conftest.py` 等测试夹具 | 真实手机号 / 姓名 | `13800000000` / `张三` |
| `README.md` | 真实姓名 | 已去掉 |
| `design/UI稿-还原/script.js` | 真实姓名 / 学校 | 已替换 |

`.env`（含真 key）和 `data/*.db`（含手机号）本来就在 `.gitignore` 里，已用 `git check-ignore` 逐条确认过。

---

## 九、已知风险 / 我需要你补的输入

- **简历基线还是去年那版**：`求职助手/00-档案/简历基线.md` 没更新。之前生成的三份简历我按你说的「先留着当参考」，没当成品。**要出新简历，得先把这一年的变化告诉我**（上次问过、还没答的那题）。
- **几个数字的口径没确认**：日活 1400（功能级还是模块级）、复用率 90%（代码行还是接口）、准确率 99%（样本区间）。现在题库和面试都会拿这三个数字追着你问——这正是我们想要的效果，但上考场前你得先把口径想清楚。
- **`data/job_assistant.db` 里有测试数据**（3 个岗位、3 份简历、45 道题、3 场面试，都是 e2e 真跑出来的）。想清空重来：删掉 `data/job_assistant.db` 后重启即可，数据库会自动重建。

---

## 七、部署到阿里云（多用户版）

### 这次改动的原因

原本是**单用户本地工具**：没有任何鉴权，谁打开网址谁就能看简历、烧 API 额度。
要放到公网给几个朋友用，必须补上账号体系和数据隔离。

### 数据隔离：一人一个数据库文件

| | 方案 A：每张表加 user_id | 方案 B：一人一个 DB 文件 ✅ 采用 |
|---|---|---|
| 改动量 | 16 张表加字段 + **~100 条手写 SQL 加 WHERE** | 只改「连接哪个库」一处 |
| 漏一处 | **数据泄露**（朋友看到你的简历） | 不可能漏，物理隔离 |
| 业务代码 | 大改 | **一行没动** |

做法：当前用户 id 放进 `ContextVar`，中间件从 cookie 里解出来，
`db.session()` 按它选库。所以 57 处业务查询全部原样保留。

### 补上的东西

| 项 | 说明 |
|---|---|
| 登录 / 注册 / 退出 | 签名 cookie（HMAC-SHA256），30 天有效，篡改和过期都会被拒 |
| 密码存储 | pbkdf2-hmac-sha256，120000 轮，每人独立 salt |
| **每日用量配额** | 默认每人 300 次/天，按自然日重置。共用一把 key 时这是唯一的刹车 |
| 老数据认领 | 升级后原 `job_assistant.db` 自动交给第一个管理员，**实测 195 条记录完整迁移** |
| 修掉硬编码路径 | 原来写死了 `/Users/wjx/...`，换机器就崩；现在支持 `WORKSPACE_DIR` 等环境变量 |
| **环境变量读取** | 原来 `_resolve()` 只认字典里已有的键，Docker 传进来的 `SECRET_KEY` 等**全被忽略**——这是部署会直接踩的坑 |

### 容器化

```
Dockerfile            python:3.13-slim + 健康检查
docker-compose.yml    app（内网）+ caddy（唯一对外入口）
Caddyfile.ip          备案前：IP + 自签证书
Caddyfile.domain      备案后：域名 + Let's Encrypt 真证书
.env.example          所有配置项，含生成 SECRET_KEY 的命令
部署文档.md           269 行，从买服务器到日常运维
```

`Caddyfile` 里有一行是必须的：

```
reverse_proxy app:7870 {
    flush_interval -1     # 不关掉缓冲，流式语音的首字节优势就没了，又变回 3 秒
}
```

### 安全边界（已做 / 未做）

已做：登录门禁、密码哈希加盐、**数据物理隔离**、key 只在服务端、
每日配额、HTTPS、安全响应头、日志限 10MB×3。

未做（已在部署文档里列明）：找回密码、登录失败次数限制、
开放注册期间任何人可注册（给了关闭方法）、上传文件病毒扫描。

### 新增 33 条测试

其中 5 条专测数据隔离（两个人互相看不到对方的岗位/简历/材料），
9 条测会话安全（伪造 cookie、改签名、过期、乱码全部拒绝）。
反向验证过：把鉴权中间件关掉，其中 3 条立刻失败。

**测试总数 250 passed。**

```sh
cd product/job_assistant && .venv/bin/python -m pytest
# 250 passed in 14.56s
```

---

## 八、第二十轮：登录改成手机号验证码

### 为什么改

原本是用户名 + 密码（pbkdf2 加盐哈希）。改成手机号验证码，实现参考了
`video_note_product/video-note/sms.py` 的成熟方案——短信配置变量名都一样，
可以直接复用那套阿里云资质。

**密码逻辑是彻底删掉的**，不是并存：`password_hash` 列、`set_password()`、
`/api/auth/register`、`_verify_password()` 全部移除。留着就是后门。

### 短信侧（照搬 video-note 的思路）

| 项 | 值 |
|---|---|
| 验证码 | 6 位数字 |
| 有效期 | 5 分钟 |
| 同号重发间隔 | 60 秒（界面倒计时与它对齐） |
| **同号每小时上限** | **8 条**（60 秒间隔挡不住"每分钟一条刷一小时"） |
| 一次性 | 用过即废 |
| 重新发送 | **作废上一条**（否则用户输旧的会莫名失败） |
| 没配短信时 | 验证码打印到**服务端日志**，不通过接口返回 |

最后一条是关键的取舍：日志只有能 SSH 上服务器的人看得到，
比"把验证码回给前端"安全得多。本机开发想图方便可以开 `SMS_DEV_MODE=1`。

### ★ 中途撞上的问题：数据被锁在门外

迁移时手机号默认绑给**管理员**账号。但实测发现——

```
#1  138****0000   管理员        数据：0
#3  imported-3    jiaxiong      数据：1 岗位 / 1 简历 / 1 材料 / 1 面试 / 12 题
```

用户下午注册的普通账号 `jiaxiong` 里是**真实使用记录**（简历「agent开发工程师」、
岗位「支付宝·AI应用工程师」、一场已完成的技术一面），手机号却绑给了空的管理员账号。
等于数据还在，人进不去了。

处理：
1. 用 `bind_phone(user_id, phone)` 把手机号改绑到有数据的账号（原号主自动腾退）
2. **迁移函数现在会报告所有"有数据但没手机号"的账号**，启动日志里逐条喊出来
3. 每次启动都复查一遍，不只是迁移那一次

反向验证过：把孤儿上报去掉，测试立刻报
`assert 'jiaxiong' in []`。

### 被锁在门外时的三条后路（写进部署文档）

1. 看服务器日志里的验证码
2. `bind_phone()` 改绑手机号
3. 直接从 `sms_code` 表里读验证码

### 实测（公网 `https://resume.videotonote.cn`）

| 测试 | 结果 |
|---|---|
| 真实短信发送 | ✅ 签名「杭州余杭智记信息技术」，模板 `SMS_338215165` |
| 手机号登录 | ✅ 登录为 jiaxiong，管理员权限 |
| 登录后数据 | ✅ 岗位 1 · 简历 1 · 材料 1 · 面试 1 · 题目 12 |
| 错误/空验证码 | ✅ 401 |
| 别人手机号 + 你的码 | ✅ 401 |
| 已用过的验证码 | ✅ 401 |
| 用户名密码登录 | ✅ 422（彻底没了） |
| 未登录读数据 | ✅ 401 |
| `trade.videotonote.cn` | ✅ 200，未受影响 |

**测试总数 258 passed。**

```sh
cd product/job_assistant && .venv/bin/python -m pytest
# 258 passed in 11.16s
```
